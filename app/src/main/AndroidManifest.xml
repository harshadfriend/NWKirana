<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="karjatonline.nw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".AddCustomer"
            android:label="Add New Customer" />
        <activity
            android:name=".NewPledge"
            android:label="New Pledge" />
        <activity
            android:name=".CustomerDetails"
            android:label="Customer Details" />
        <activity
            android:name=".NewTransaction"
            android:label="New Transaction" />
        <activity
            android:name=".PledgeDetails"
            android:label="Pledge Details" />
        <activity android:name=".MonthCalculate" />
        <activity
            android:name=".DeleteUser"
            android:label="Delete User" />
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PledgeReport"
            android:label="Pledge Report" />
        <activity android:name=".TransactionReport"
            android:label="Transaction Report"/>
        <activity android:name=".EditUser"
            android:label="Edit User"/>
        <activity android:name=".EditUserDialog"
            android:label="Edit"
            android:theme="@style/Theme.AppCompat.Light.Dialog"></activity>
    </application>

</manifest>